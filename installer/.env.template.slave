# Homelab Slave Node Configuration Template
# Copy this file to ~/homelab/.env and customize for your slave node setup

# Node Configuration (required for slave-node)
NODE_TYPE=slave-node
NODE_NAME=slave-node-1
MASTER_IP=
CLUSTER_TOKEN=

# Optional System Configuration (has sensible defaults)
#DEFAULT_TIMEZONE=UTC
#SSH_PORT=22

# Optional Performance Tuning (usually defaults are fine for slave nodes)
#VM_SWAPPINESS=10
#MAX_FILE_DESCRIPTORS=655350
#INOTIFY_WATCHES=524288

# Installation Options
#AUTO_CONFIRM=false
#VERBOSE_MODE=false

# SSH Key Management (recommended for admin access)
#ENABLE_SSH_KEY_SYNC=true
#GITHUB_KEYS_URL=https://github.com/razumnyak.keys
#SSH_KEY_SYNC_HOUR=3